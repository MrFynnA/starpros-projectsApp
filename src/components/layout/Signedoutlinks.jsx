import React from "react";
import { NavLink } from "react-router-dom";



const SignOutLinks=()=>{


    return(
        // <ul className="flex items-center gap-5 text-white">
        // <li><NavLink to={'signup'} className={({isActive})=>isActive?'!text-blue-400 font-bold':'font-bold'}>Sign Up</NavLink>
        // </li>

        // <li><NavLink className={({isActive})=>isActive?'!text-blue-400 font-bold':'font-bold'} to={'/signin'}>Log in</NavLink></li>

        // </ul>
        ''
     
    )
}

export default SignOutLinks